// Plain object export — evita problemas de resolução do export ESM
// `defineCliConfig` (que é apenas um helper de tipagem).
export default {
  api: {
    projectId: 'h8odpb0f',
    dataset: 'production',
  },
  studioHost: 'memoria-cavaquinho',
}
